# Repository Instructions

- Commit each completed action as a separate Git commit before beginning the next action.
- Keep each commit scoped to that action and exclude unrelated working-tree changes.
